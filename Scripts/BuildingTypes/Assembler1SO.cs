using UnityEngine;

namespace BuildingTypes
{
	[CreateAssetMenu(fileName = "Assembler1SO", menuName = "ScriptableObjects/Assembler/Assembler1")]
	public class Assembler1SO : PlaceableObjectSO
	{
		// Start is called before the first frame update
		void Start()
		{
        
		}

		// Update is called once per frame
		void Update()
		{
        
		}
	}
}
