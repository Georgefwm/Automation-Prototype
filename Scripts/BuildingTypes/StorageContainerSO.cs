using UnityEngine;

namespace BuildingTypes
{
	[CreateAssetMenu(fileName = "StorageContainerSO", menuName = "ScriptableObjects/StorageContainer")]
	public class StorageContainerSO : PlaceableObjectSO
	{
		// Start is called before the first frame update
		void Start()
		{
        
		}

		// Update is called once per frame
		void Update()
		{
        
		}
	}
}
